# Problem
Factor `x^2 - 5x + 6`.

# Solution
Look for two numbers that:

- multiply to `6`, and
- add to `-5`.

The numbers are `-2` and `-3`, so:

`x^2 - 5x + 6 = (x - 2)(x - 3)`

The roots of the quadratic are therefore `x = 2` and `x = 3`.
