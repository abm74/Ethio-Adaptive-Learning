A limit asks what value a function is getting close to as the input gets close to a target.

In notation:

`\\lim_{x \\to a} f(x)`

means "the value `f(x)` approaches as `x` approaches `a`."

This matters because calculus often studies local behavior near a point, not just the value exactly at that point.
